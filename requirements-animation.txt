# Optional, for Animation mode only.
#
#     pip install -r requirements-animation.txt
#
# Kept out of requirements.txt on purpose for every platform but macOS, where
# it is already pulled in for a different reason: live-action embedding runs
# through its CoreML provider there. onnxruntime is 80 MB installed, so on
# Windows and Linux -- where live-action mode does not use it -- a user who
# only processes filmed footage should not carry it.
#
# This is also why animation mode reports itself unavailable rather than
# failing at the first frame: see app/modes.availability.
onnxruntime==1.29.0
